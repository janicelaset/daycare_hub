if ("<%= @error %>")
  alert("error");
else {
  $('#collapseThree').collapse("hide");
  $('#contact-add-edit-glyph').html('<%= j render 'contacts/add_edit_glyph' %>')
  $('#contact-edit-form').html('<%= j render 'contacts/form' %>')
}
