<%= render "layouts/errors", :object => @daycare %>

<%= form_for [current_user, @daycare] do |f|%>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label 'Type' %>
  <%= f.text_field :structure %>

  <%= f.submit %>
<% end %>
