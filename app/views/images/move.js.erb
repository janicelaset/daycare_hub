
$("#image-add-list").html("<%= j render 'images/image_list' %>")
$('#imageList').sortable({
    axis: 'y',
    panel: '.panel-heading',
    update: function() {
      $.post($(this).data('update-url'), $(this).sortable('serialize'))
    }
  }
);
