$('#program-add-edit-glyph').html('<%= j render 'programs/add_edit_glyph' %>');
$('#program-edit-list').html('<%= j render 'programs/program_list' %>');
$('#program-edit-form').html('<%= j render 'programs/form' %>');

$('#programList').sortable({
    axis: 'y',
    panel: '.panel-heading',
    update: function() {
      $.post($(this).data('update-url'), $(this).sortable('serialize'))
    }
  }
);
