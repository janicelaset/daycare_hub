if ("<%= @error %>")
  alert("error");
else {
  $('#collapseFour').removeClass('collapse in').addClass('collapse');
  $('#summary-add-edit-glyph').html('<%= j render 'summaries/add_edit_glyph' %>')
  $('#summary-edit-form').html('<%= j render 'summaries/form' %>')
}
