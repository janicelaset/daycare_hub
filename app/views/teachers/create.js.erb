$('#teacher-add-edit-glyph').html('<%= j render 'teachers/add_edit_glyph' %>');
$('#teacher-edit-list').html('<%= j render 'teachers/teacher_list' %>');
$('#teacher-edit-name<%= @teacher.id %>').val("");
$('#teacher-edit-about<%= @teacher.id %>').val("");
$('#teacher-upload-file-info').html("");

$('#teacherList').sortable({
    axis: 'y',
    panel: '.panel-heading',
    update: function() {
      $.post($(this).data('update-url'), $(this).sortable('serialize'))
    }
  }
);
